(menhir
 (modules Javert_Parser)
 (flags --explain))

(ocamllex
 (modules Javert_Lexer))

(library
 (name parsing)
 (public_name gillian-js-libs.parsing)
 (libraries gillian jsil_syntax jslogic javert_utils str))
