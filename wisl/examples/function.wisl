function double(x : Int) {
  x + x
}

function triple(x : Int) {
 x + x + x
}

{ x == #x }
proc times_two(x) {
  y := x * 2;
  return y
}
{ ret == double(#x) }

{ x == #x }
proc times_three(x) {
  y := x * 3;
  return y
}
{ ret == triple(#x) }

// This spec fails to verify
{ x == #x }
proc times_four(x) {
  y := x * 4;
  return y
}
{ ret == triple(#x) }
